name: nm
description: OEMS24 Captain App
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  get: ^4.6.5
  permission_handler: ^11.2.0
  geolocator: ^11.0.0
  google_maps_flutter: ^2.5.3
  flutter_compass: ^0.8.0
  image_picker: ^1.0.7
  flutter_map: ^6.1.0
  latlong2: ^0.9.0
  flutter_map_location_marker: ^8.0.4
  badges: ^3.1.2
  intl: ^0.19.0
  google_fonts: ^6.1.0
  qr_flutter: ^4.1.0
  pin_code_fields: ^8.0.1
  cached_network_image: ^3.3.1
  url_launcher: ^6.2.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
